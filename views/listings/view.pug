extends ../layout

block content


  div(class='viewListing')
    h2= showlisting.name
    div(id='listing-title')=showlisting.title
    div(id='listing-description')=showlisting.description
    div(id='listing-location')=showlisting.location
    div(id='listing-price')='£' + showlisting.price + ' per night'


  br

  form(class="request-form" name="submit-request", action="/requests/confirm?id=" + showlisting.id, method="post")
    h2 Book stay
    div.input
      span.label Start Date
      br
      input(id="start-date-input", type="date", name="start-date")
      br
      br
      span.label End Date
      br
      input(id="end-date-input", type="date", name="end-date")
      br
    div.actions
      br
      input(type="submit", value="Request booking")
