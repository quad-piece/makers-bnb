extends ../layout

block content


  div(class='viewListing')
    h2= showlisting.name
    div(id='listing-title')=showlisting.title
    div(id='listing-description')=showlisting.description
    div(id='listing-location')=showlisting.location
    div(id='listing-price')='£' + showlisting.price + ' per night'


  br
  h2 Book stay

  form(id="request-form" name="submit-request", action="/requests/confirm?id=" + showlisting.id, method="post")
    div.input
      span.label Start Date
      br
      input(class="start-date-input", type="date", name="start-date")
      br
      br
      span.label End Date
      br
      input(class="end-date-input", type="date", name="end-date")
      br
      br
      span.label Price
    div.actions
      br
      input(type="submit", value="Request booking")
